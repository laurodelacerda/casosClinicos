= semantic_form_for [@doencatratamento] do |form|
 
  = form.input :doenca, :member_label => :nomepopular, :label => 'Nome Popular'
  = form.input :tratamento, :member_label => :metodo, :label => 'Método'
  = form.submit 'Salvar'
